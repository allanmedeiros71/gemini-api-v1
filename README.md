# gemini-api-v1

Example System for Studying Python with the Gemini API

## Resumo rápido e passos para rodar (Português):

```shell
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install -r requirements.txt
python3 gemini.py
```
